I want you to extend my current admin dashboard so I can fully manage all service listings for my travel platform Tembea Bila Matata. IMPORTANT: The official platform categories must remain exactly as follows: stays cars cooks errands Do NOT rename or replace these categories. ✅ 1. Add “Listings” to the Admin Menu In the admin navigation, add: Listings Add Listing ✅ 2. Create /admin/listings page A table showing all listings loaded from the database using React Query (/api/admin/listings). Table columns: Title Category (stays, cars, cooks, errands) Price Location Edit button Delete button Use shadcn/ui table components. ✅ 3. Create “Add Listing” Page → /admin/listings/new A form with these fields: Title (text) Category (dropdown: stays, cars, cooks, errands) Price (number) Location (text) Image URL (text) Description (textarea) Features (checkbox list: WiFi, Hot Shower, Parking, Kitchen, Smart TV, Driver, etc.) On submit: POST /api/admin/listings Redirect back to /admin/listings ✅ 4. Create “Edit Listing” Page → /admin/listings/:id/edit Load listing using GET /api/admin/listings/:id. Pre-fill the same form. On Save: PUT /api/admin/listings/:id ✅ 5. Enable Delete Listing Each row in the listings table should have a Delete button. On click: Show confirmation modal If confirmed: DELETE /api/admin/listings/:id Refresh table ✅ 6. Backend API Routes Required Add or confirm these routes: GET /api/admin/listings POST /api/admin/listings GET /api/admin/listings/:id PUT /api/admin/listings/:id DELETE /api/admin/listings/:id Data must be stored in the database. ✅ 7. Database Schema for Listings Add/update the Listing model to: { id: string, title: string, category: "stays" | "cars" | "cooks" | "errands", price: number, location: string, imageUrl: string, description: string, features: string[], createdAt: string, updatedAt: string } ✅ 8. Frontend requirements Use shadcn/ui cards, forms, tables Match styling of existing admin layout Responsive for mobile Add color badges for categories Proper loading states (skeletons) ✅ Final Objective Build a complete CRUD system for managing all listings under the four official categories: Stays, Cars, Cooks, Errands. The admin must be able to: Add listings Edit listings Delete listings View all listings All pages must be integrated into the existing admin layout and backend architecture.